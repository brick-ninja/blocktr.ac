describe("XRPTxSummary", () => {
  describe("dom", () => {
    test.todo("renders div title")

    describe("tx_type == 'Payment'", () => {
      test.todo("renders PaymentTx")
    })

    describe("tx_type == 'OfferCreate'", () => {
      test.todo("renders OfferCreateTx")
    })

    describe("tx_type == 'OfferCancel'", () => {
      test.todo("renders OfferCancelTx")
    })

    describe("tx_type == 'EscrowCreate'", () => {
      test.todo("renders EscrowCreateTx")
    })

    describe("tx_type == 'EscrowCancel'", () => {
      test.todo("renders EscrowCancelTx")
    })

    describe("tx_type == 'EscrowFinish'", () => {
      test.todo("renders EscrowFinishTx")
    })

    describe("tx_type == 'TrustSet'", () => {
      test.todo("renders TrustSetTx")
    })

    describe("tx_type == 'SignerListSet'", () => {
      test.todo("renders SignerListSetTx")
    })

    describe("tx_type == 'AccountSet'", () => {
      test.todo("renders AccountSetTx")
    })

    describe("tx_type == 'AccountDelete'", () => {
      test.todo("renders AccountDeleteTx")
    })

    describe("tx_type == 'PaymentChannelClaim'", () => {
      test.todo("renders PaymentChannelClaimTx")
    })

    describe("tx_type == 'PaymentChannelCreate'", () => {
      test.todo("renders PaymentChannelCreateTx")
    })

    describe("tx_type == 'PaymentChannelFund'", () => {
      test.todo("renders PaymentChannelFundTx")
    })
  })

  describe("computed", () => {
    describe("#title", () => {
      test.todo("tx_type @ formatted_data")
    })
  })
})
