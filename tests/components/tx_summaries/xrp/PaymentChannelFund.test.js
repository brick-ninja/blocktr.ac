describe("PaymentChannelClaim", () => {
  describe("dom", () => {
    describe("mq >= md", () => {
      test.todo("renders destination")
    })
  })

  describe("computed", () => {
    describe("paychan", () => {
      test.todo("is PayChannel modified_fields")
    })

    describe("amount", () => {
      test.todo("is tx_obj['Amount']")
    })

    describe("dst", () => {
      describe("paychan is null", () => {
        test.todo("is null")
      })

      test.todo("is paychan['Destination']")
    })
  })
})
