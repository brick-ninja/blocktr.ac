describe("TxCategoriesDropdown", () => {
  describe("dom", () => {
    test.todo("ties categories to dropdown items")

    test.todo("renders category icon")

    test.todo("renders lower case category name")
  })

  describe("computed", () => {
    describe("categories", () => {
      test.todo("all categories except for 'ALl'")
    })
  })

  describe("watch", () => {
    describe("selected_categories", () => {
      describe("new categories is empty", () => {
        test.todo("clears tx categories")
      })

      test.todo("toggles added categories")
      test.todo("toggles removed categories")
    })
  })
})
