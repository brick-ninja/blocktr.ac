describe("TxsFilterExample", () => {
  describe("dom", () => {
    describe("#txs_filter_example_jsonpath", () => {
      describe("span", () => {
        test.todo("renders example")

        describe("click event", () => {
          test.todo("sets filter from example")
        })
      })

      describe("img", () => {
        test.todo("launched txs_filter_help modal")
      })
    })
  })

  describe("computed", () => {
    test.todo("is network json path example")
  })

  describe("methods", () => {
    describe("set_filter", () => {
      test.todo("updates tx filter")
    })
  })
})
