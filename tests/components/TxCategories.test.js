describe("TxCategories", () => {
  describe("dom", () => {
    test.todo("renders primary categories")

    test.todo("renders secondary categories")

    describe("list-group-item", () => {
      describe("category is enabled", () => {
        test.todo("is enabled")
      })

      describe("click event", () => {
        test.todo("toggles category")
      })
    })

    describe("primary categories", () => {
      test.todo("renders category_icon")

      describe(".category_name", () => {
        test.todo("renders lower case category name")

        describe("category is enabled", () => {
          test.todo("is enabled")
        })
      })
    })

    describe("secondary categories", () => {
      test.todo("renders category_icon")

      describe("tooltip", () => {
        test.todo("renders lower case category name")
      })
    })

    describe(".category_tally", () => {
      test.todo("renders category tally")
    })
  })
})
