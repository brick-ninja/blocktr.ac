describe("BatchSettings", () => {
  describe("dom", () => {
    describe("form-radio-group", () => {
      test.todo("tied to batch_size")
      test.todo("ties batch_sizes to options")
    })

    test.todo("renders current batch_size")
  })

  describe("computed", () => {
    describe("batch_sizes", () => {
      test.todo("is membership_features batch_sizes")
      test.todo("contains batch_size privilege")
    })
  })

  describe("#created", () => {
    test.todo("assigns profile.batch_size to batch_size")
  })
})
