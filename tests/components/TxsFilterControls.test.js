describe("TxFilterControls", () => {
  describe("dom", () => {
    describe("txs_filter_clear", () => {
      describe("!filter", () => {
        test.todo("is disabled")
      })

      describe("click event", () => {
        test.todo("sets filter = ''")
      })
    })

    describe("txs_filter_Save", () => {
      describe("!filter", () => {
        test.todo("is disabled")
      })

      test.todo("launches save_filter modal")
    })
  })

  describe("computed", () => {
    describe("filter", () => {
      test.todo("gets store.tx_filter")
      test.todo("updates tx filter")
    })
  })
})
