describe("has_filters", () => {
  describe("computed", () => {
    describe("#filters", () => {
      test.todo("returns filters")
    })

    describe("#remaining_filters", () => {
      test.todo("authorized_filters - filter.length")
    })

    describe("#remaining_filters_msg", () => {
      describe("1 remaining filter", () => {
        test.todo("is '1 filter is left'")
      })

      test.todo("is 'N filters are left'")
    })
  })
})
