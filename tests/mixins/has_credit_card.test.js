describe("has_credit_card", () => {
  describe("computed", () => {
    describe("#credit_card_number_with_dashes", () => {
      describe("!credit_card_number", () => {
        test.todo("is ''")
      })

      test.todo("is credit card number with dashes every 4 characters")
    })

    describe("#have_credit_card_number", () => {
      describe("have credit card number", () => {
        test.todo("is true")
      })

      describe("do not have credit card number", () => {
        test.todo("is false")
      })
    })

    describe("#have_credit_card_cvc", () => {
      describe("have credit card cvc", () => {
        test.todo("is true")
      })

      describe("do not have credit card cvc", () => {
        test.todo("is false")
      })
    })

    describe("#invalid_credit_card_cvc", () => {
      describe("credit card cvc invalid", () => {
        test.todo("is true")
      })

      describe("credit card cvc valid", () => {
        test.todo("is false")
      })
    })

    describe("#credit_card_params", () => {
      test.todo("is credit card params")
    })
  })

  describe("methods", () => {
    describe("#set_credit_card_number", () => {
      test.todo("sets credit card number w/out dashes")
    })
  })
})
